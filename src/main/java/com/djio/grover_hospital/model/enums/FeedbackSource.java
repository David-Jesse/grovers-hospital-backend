package com.djio.grover_hospital.model.enums;

public enum FeedbackSource {
    /** Submitted via the public homepage form (no auth) */
    HOMEPAGE,
    /** Submitted from inside the patient portal (logged-in patient) */
    PORTAL
}
